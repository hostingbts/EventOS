// Copy this file to Secrets.swift (gitignored) and fill in real values.
// Same values as VITE_API_URL / VITE_API_TOKEN in web/.env.local.
enum Secrets {
    static let apiURL = "https://script.google.com/macros/s/YOUR_DEPLOYMENT_ID/exec"
    static let apiToken = "your-long-random-token"
}
