/** Public asset paths (respect GitHub Pages base URL). */
const base = import.meta.env.BASE_URL;

export const LOGO_ICON = `${base}logo-icon.png`;
export const LOGO_WORDMARK = `${base}logo-wordmark.png`;
